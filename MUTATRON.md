# 诱变机使用说明

该功能用于自动执行带 `dimension` 条件的蜜蜂突变。默认不会把普通突变送入诱变机；将 `config.lua` 的 `mutatron.mode` 改为 `"all"` 才会这样做。

## 站点布置

以机器人启动时所在 OC 充电器为原点。默认配置要求：

- 当前机器人朝西时，Mutatron 位于正前方的 `(0, 1, -1)`。
- 机器人操作时站在机器正上方的 `(0, 2, -1)`。
- Mutatron 要预先接好电力和 Mutagen。
- ME 网络需要能提供 `gendustry:Labware`。

若实际位置不同，只修改 `config.lua` 的 `mutatron.position` 和 `robotY`。机器人从机器下方一面访问物品栏，因此默认 `machineSide = 0` 不应改动。

## 工作方式

Gendustry 的槽位顺序为：公主蜂 1、雄蜂 2、输出 3、Labware 4。诱变机产出的是蜂后，不是公主蜂；脚本会将蜂后送回现有蜂箱流程，取得下一代公主蜂和雄蜂后继续提纯。

诱变机忽略普通突变的维度、基石、群系、温湿度、时间等条件，所以维度蜂不需要再移动机器人到对应星球。机器不处理 `disabledMutatron = true` 的记录；这些突变仍保留原来的人工流程。

基础 Mutatron 在同一对亲本有多条可用突变时会随机选择结果。脚本会在结果不是目标物种时停止并保留输出蜂后，避免把错误物种继续送进提纯流程。此类亲本组合应使用高级诱变机或改为唯一配对。

## 已知限制

- GTNH Wiki 列出的黑名单蜂会被脚本保守地留在人工路径，包括节日蜂、Chad、镅、铕、凯芙拉、Drake、龙血、宇宙中子态素、无尽催化剂和无尽。Wiki 中标有 `(R)` 的部分蜂种在满足原始条件时可能仍可手动送入诱变机；本脚本不会自动这样做。
- 始祖公主蜂经诱变机可能变为卑贱；卑贱公主蜂可能产出 `gendustry:Waste`。脚本会停止并保留当前输出，避免静默丢失状态。
- 机器输入或输出槽里遗留物品时，脚本会先报错，不会覆盖已有物品。
